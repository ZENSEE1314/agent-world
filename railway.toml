[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Rely on the Dockerfile CMD (runs via sh -c so $PORT expands).
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
healthcheckPath = "/api/healthz"
healthcheckTimeout = 60
